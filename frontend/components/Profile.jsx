import React from 'react';
import gql from 'graphql-tag';

import FormContainer from './FormContainer';

const QUERY_ME = gql``;

const Profile = () => {
  return <FormContainer>This is the profile page</FormContainer>;
};

export default Profile;
